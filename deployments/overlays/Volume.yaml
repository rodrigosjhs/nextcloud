---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${JOB_NAME}
  namespace: intranet
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs2-csi-prd
  resources:
    requests:
      storage: 150Gi
